# ===================================================================
#  JetBrains / CLion IDE specific files
# ===================================================================
# CLion 项目配置文件夹，包含你的个人窗口布局、运行配置等。
# 这必须被忽略，因为它对其他开发者是无用的。
.idea/

# CLion 在某些情况下生成的 CMake 用户配置文件
cmake-build-*/CMakeCache.txt
cmake-build-*/CMakeFiles/
cmake-build-*/cmake_install.cmake
cmake-build-*/Makefile

# ===================================================================
#  Build artifacts (构建产物)
# ===================================================================
# 忽略所有名为 cmake-build-* 的目录，这是 CLion 默认的构建输出目录。
# 比如你的 cmake-build-debug 就会被这条规则匹配到。
cmake-build-*/

# 有时人们会手动创建 build 目录
build/

# ===================================================================
#  General compiled files (通用编译文件)
# ===================================================================
# Object files (目标文件)
*.o
*.obj

# Libraries (库文件)
*.a
*.lib
*.so
*.dll

# Executables (可执行文件)
*.exe
*.out
*.app

# ===================================================================
#  Other (其他)
# ===================================================================
# macOS 系统文件
.DS_Store

# 编辑器临时文件或备份文件
*~
*.swp
*.swo